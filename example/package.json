{
  "name": "example",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "detox:ios:build": "detox build -c ios.release",
    "detox:ios:test": "detox test -c ios.release",
    "detox:ios": "npm run detox:ios:build && npm run detox:ios:test",
    "detox:android:build": "detox build -c android.release",
    "detox:android:test": "detox test -c android.release",
    "detox:android": "npm run detox:android:build && npm run detox:android:test"
  },
  "dependencies": {
    "expo": "~54.0.27",
    "expo-device": "~8.0.10",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "expo-native-launch-arguments": "file:.."
  },
  "devDependencies": {
    "@types/react": "~19.1.0",
    "detox": "^20.34.2",
    "jest": "^29.7.0",
    "typescript": "~5.9.2"
  },
  "private": true
}
